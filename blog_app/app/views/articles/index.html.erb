<% @articles.each do |article| %>
  <p>
    <%= notice %>
  </p>
  <p>
  <%= link_to "Create New Article", new_article_path %>
  </p>
  <%= link_to show_article_path(article.id) do %>
    Got to Article
  <% end %>
  <%= render "article", article: article %>
<% end %>